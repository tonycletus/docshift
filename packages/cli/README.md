# DocShift CLI

Local-first PDF tools for the terminal.

```sh
npm install -g @tonycletus/docshift
docshift doctor
docshift compress input.pdf --preset balanced -o output.pdf
```

No account, database, upload queue, or external API is required.
